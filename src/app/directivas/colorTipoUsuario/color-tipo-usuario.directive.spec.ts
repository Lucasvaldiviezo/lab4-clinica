import { ColorTipoUsuarioDirective } from './color-tipo-usuario.directive';

describe('ColorTipoUsuarioDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTipoUsuarioDirective();
    expect(directive).toBeTruthy();
  });
});
